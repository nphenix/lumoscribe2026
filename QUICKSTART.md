# Lumoscribe2026 快速使用指南

> 基于 Agent Skills 的智能化项目开发脚手架

## 📋 目录

- [项目概述](#项目概述)
- [核心概念](#核心概念)
- [工作流程](#工作流程)
- [快速上手](#快速上手)
- [常用命令](#常用命令)
- [目录结构](#目录结构)
- [模板系统](#模板系统)

---

## 项目概述

**Lumoscribe2026** 是一个遵循 Agent Skills 标准的项目开发脚手架，提供完整的开发流程管理能力：

- ✅ **规范驱动**: 所有开发活动遵循统一的规范和模板
- ✅ **AI 协作**: 通过 Skills 系统与 AI 助手高效协作
- ✅ **可追溯**: 完整的工件管理，确保开发过程可追溯
- ✅ **多 IDE 支持**: 支持 Cursor、Roo Code、Trae 等主流 AI IDE

---

## 核心概念

### 单一事实来源 (Source of Truth)

```
speckit/                    # 核心规范目录
├── skills/                 # 技能定义库
├── templates/              # 模板文件
└── specs/                  # 规格文档
```

**规则**: 所有规范、模板、技能定义都以 `speckit/` 为单一事实来源。

### 关键路径 (Critical Pathway)

```
spec → plan → task → change → review → commit → debug
```

| 阶段 | 产出 | 状态 |
|------|------|------|
| `spec` | 功能规格 | DRAFT → REVIEWED |
| `plan` | 实施计划 | DRAFT → APPROVED |
| `task` | 任务分解 | PENDING → IN_PROGRESS → COMPLETED |
| `change` | 变更记录 | DRAFT → REVIEWED → MERGED |
| `review` | 审查报告 | PENDING → APPROVED |
| `commit` | Git 提交 | PENDING → COMMITTED |
| `debug` | 调试报告 | IDENTIFIED → RESOLVED |

---

## 工作流程

### 完整功能开发流程

```mermaid
graph LR
    A[需求] --> B[spec-generator]
    B --> C[speckit/specs/features/xxx/spec.md]
    C --> D[评审]
    D --> E[plan-creator]
    E --> F[speckit/specs/features/xxx/plan.md]
    F --> G[评审]
    G --> H[task-breakdown]
    H --> I[speckit/specs/features/xxx/tasks.md]
    I --> J[实施任务]
    J --> K[change-manager]
    K --> L[code-review]
    L --> M[git-manager]
    M --> N[git commit]
```

### 场景匹配

| 场景 | 推荐流程 | 说明 |
|------|----------|------|
| **原型探索** | spec + tasks（简化版） | 快速验证，任务≤5个 |
| **功能开发** | spec → plan → tasks | 完整流程，长期维护 |
| **缺陷修复** | debug + change | 问题定位，快速交付 |
| **重大重构** | plan + change | 架构调整，兼容性策略 |

---

## 快速上手

### 步骤 1: 定义需求

用自然语言描述你的需求：

> "请为用户认证功能生成规格"
> "我想开发一个任务管理模块"

### 步骤 2: 生成规格 (spec)

AI 助手会自动激活 `spec-generator` 技能，生成：

```
speckit/specs/features/auth/spec.md
```

**必需内容**: 规格文档必须包含 `## API` 章节。

### 步骤 3: 制定计划 (plan)

规格评审通过后，生成实施计划：

> "为用户认证功能创建计划"

输出：

```
speckit/specs/features/auth/plan.md
```

### 步骤 4: 分解任务 (task)

计划批准后，分解为可执行任务：

> "将用户认证功能分解为任务"

输出：

```
speckit/specs/features/auth/tasks.md
```

### 步骤 5: 实施与记录

按任务实施，记录变更：

> "记录我的代码变更"

### 步骤 6: 代码审查

提交审查：

> "审查我的代码"

### 步骤 7: 提交代码

生成提交信息：

> "生成提交信息"

---

## 常用命令

### 自然语言命令

直接用自然语言描述需求，AI 助手会自动匹配 Skills：

| 场景 | 命令示例 |
|------|----------|
| 生成规格 | `"请为 [功能名] 生成规格"` |
| 创建计划 | `"为 [功能名] 创建计划"` |
| 分解任务 | `"为 [功能名] 分解任务"` |
| 记录变更 | `"记录变更：[描述]"` |
| 代码审查 | `"审查 [变更ID]"` |
| 生成提交 | `"生成提交信息"` |
| 调试问题 | `"调试 [错误描述]"` |

### 同步命令

```powershell
# 同步技能到 IDE 配置
pwsh scripts/sync-skills.ps1

# 查看 Git 状态
git status

# 提交变更
git add . && git commit -m "描述"
```

---

## 目录结构

```
.
├── speckit/                    # 核心规范目录
│   ├── skills/                 # 技能定义
│   │   ├── spec-generator/    # 规格生成
│   │   ├── plan-creator/      # 计划制定
│   │   ├── task-breakdown/    # 任务分解
│   │   ├── change-manager/    # 变更管理
│   │   ├── code-review/       # 代码审查
│   │   ├── git-manager/       # 提交管理
│   │   └── debug-diagnostic/  # 调试诊断
│   ├── templates/              # 模板文件
│   │   ├── spec-template.md
│   │   ├── plan-template.md
│   │   ├── tasks-template.md
│   │   ├── change-template.md
│   │   └── debug-template.md
│   ├── specs/                  # 规格文档
│   │   ├── features/          # 功能规格
│   │   ├── changes/           # 变更记录
│   │   └── debug/             # 调试报告
│   └── constitution.md         # 项目章程
├── .cursor/                    # Cursor IDE 配置
├── .roo/                       # Roo Code 配置
├── .trae/                      # Trae IDE 配置
├── scripts/                    # 工具脚本
│   └── sync-skills.ps1        # 同步脚本
└── AGENTS.md                   # Agents 规范
```

---

## 模板系统

### 可用模板

| 模板 | 文件 | 用途 |
|------|------|------|
| 规格 | `spec-template.md` | 功能需求说明 |
| 计划 | `plan-template.md` | 技术实施方案 |
| 任务 | `tasks-template.md` | 任务分解排期 |
| 变更 | `change-template.md` | 变更提案管理 |
| 调试 | `debug-template.md` | 问题诊断修复 |

### 模板裁剪规则

#### 可省略章节

| 场景 | 可省略内容 |
|------|-----------|
| 原型 | 边界与异常情况、成功标准 |
| 小型项目 | 阶段 N 完善、并行任务标记 |
| 快速修复 | 技术上下文、项目结构规划 |

#### 必填内容

| 模板 | 必填章节 |
|------|----------|
| spec | 用户场景与测试、需求 |
| plan | 摘要、技术上下文、项目结构 |
| tasks | 阶段 1/2、至少一个用户故事 |
| change | 变更原因、Delta 操作、影响范围 |

---

## IDE 配置

### Cursor 配置

规则文件位置：`.cursor/rules/`

### Roo Code 配置

规则文件位置：`.roo/rules/`

### Trae 配置

规则文件位置：`.trae/rules/`

### 同步配置

修改 `speckit/` 后，运行同步脚本：

```powershell
pwsh scripts/sync-skills.ps1
```

---

## 项目章程

所有开发活动必须遵循 [`speckit/constitution.md`](speckit/constitution.md) 中的核心原则：

- **P1**: 代码质量守则（DRY、SOLID、设计模式）
- **P2**: 目录结构与工件定位
- **P3**: 临时文件纪律
- **P4**: 文档管理
- **P5**: AI 助手行为准则
- **P6**: 错误处理规范
- **P7**: 日志规范
- **P8**: 禁止模式
- **P9**: 交互规范（自然语言优先）

---

## 最佳实践

1. ✅ **遵循关键路径**: 严格按 `spec → plan → task` 流程推进
2. ✅ **使用自然语言**: 与 AI 助手交互时使用自然语言描述需求
3. ✅ **保持文档同步**: 代码变更后及时更新相关文档
4. ✅ **单一事实来源**: 所有规范修改后运行 `sync-skills.ps1`
5. ✅ **状态标记**: 使用正确的状态标记跟踪进度
6. ✅ **路径规范**: 遵循 `speckit/specs/` 目录结构规范

---

## 常见问题

**Q: 如何开始一个新功能？**
> A: 直接用自然语言描述需求，AI 助手会自动激活相应技能。

**Q: 忘记命令格式怎么办？**
> A: 不需要记住命令，只需用自然语言描述你想要做什么。

**Q: 如何同步配置到 IDE？**
> A: 修改 `speckit/` 后，运行 `pwsh scripts/sync-skills.ps1`

**Q: 遇到错误如何调试？**
> A: 运行 `pwsh scripts/sync-skills.ps1` 重新同步配置。

---

## 下一步

1. 阅读 [项目章程](speckit/constitution.md)
2. 查看 [模板索引](speckit/templates/INDEX.md)
3. 了解 [关键路径规范](speckit/specs/PATHWAY.md)
4. 开始你的第一个项目设计

---

**最后更新**: 2026-01-15 | **版本**: 1.0.0
